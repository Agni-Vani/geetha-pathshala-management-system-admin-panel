enum AcademicYearStatus { upcoming, active, completed, archived }

enum AdmissionStatus { pending, active, transferred, graduated, withdrawn }

enum TransferStatus { requested, approved, rejected, completed, cancelled }

enum GroupStatus { active, merged, completed, archived }

enum TeacherStatus { active, onLeave, inactive, resigned }

enum AttendanceStatus { present, absent, late, excused }

enum SessionStatus { open, submitted, approved, corrected }
