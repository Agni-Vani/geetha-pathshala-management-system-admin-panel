import 'package:flutter/material.dart';

class AllPatshalaView extends StatelessWidget {
  const AllPatshalaView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}